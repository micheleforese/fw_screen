# Example Mosquitto Docker Server

1. Navigate to this folder
2. Type:

    ```shell
    docker compose up
    ```

    > For Detached mode

    ```shell
    docker compose up -d
    ```

3. To Close it

    ```shell
    docker compose down
    ```
